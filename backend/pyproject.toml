[project]
name = "veritas-backend"
version = "0.1.0"
description = "核真 Veritas 市场传闻核查助手 - 后端"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "langgraph>=0.2.50",
    "pydantic>=2.7",
    "httpx>=0.27",
    "tiktoken>=0.14.0",
]

[tool.uv]
package = false
